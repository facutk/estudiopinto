import React from 'react';
import Header from './Header';
import DNIEntry from './DNIEntry';

const Root = () => (
    <div className="root">
        <Header />
        <DNIEntry />
    </div>
);

export default Root;
